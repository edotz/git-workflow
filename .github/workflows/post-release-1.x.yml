# GENERATED — do not edit manually. Source: .github/workflow.env
name: Post Release (1.x)

on:
  pull_request:
    branches:
      - '1.x'
    types:
      - closed
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version to backport (e.g. 1.0.0)'
        required: true
      force_readme:
        description: 'Force README regeneration regardless of CHANGELOG diff'
        type: boolean
        default: false

jobs:
  post-release:
    if: >
      (github.event_name == 'pull_request' &&
       github.event.pull_request.merged == true &&
       startsWith(github.event.pull_request.head.ref, 'release-please--')) ||
      github.event_name == 'workflow_dispatch'
    uses: edotz/git-workflow/.github/workflows/_callable-post-release.yml@main
    with:
      release_branch: '1.x'
      backport_to: 'master'
      backport_files: '.release-please-manifest.json CHANGELOG.md'
      release_type: 'simple'
      project_type: 'boilerplate'
      version: ${{ inputs.version || '' }}
      force_readme: ${{ inputs.force_readme || false }}
    secrets: inherit
